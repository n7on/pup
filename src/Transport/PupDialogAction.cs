namespace Pup.Transport
{
    /// <summary>
    /// Action to take when a JavaScript dialog (alert, confirm, prompt) appears
    /// </summary>
    public enum PupDialogAction
    {
        /// <summary>
        /// Accept the dialog (click OK)
        /// </summary>
        Accept,

        /// <summary>
        /// Dismiss the dialog (click Cancel)
        /// </summary>
        Dismiss
    }
}
